# Intentionally empty.
# Lambda log groups are auto-created by AWS when the functions run.
# Step Functions logging is disabled (no CloudWatch Logs resources managed by Terraform)
# to avoid permissions like logs:ListTagsForResource / PutRetentionPolicy.
